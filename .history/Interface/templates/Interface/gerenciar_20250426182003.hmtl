<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Viagens Cadastradas - NextStop</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white min-h-screen flex items-center justify-center">

  <div class="bg-gray-800 rounded-lg shadow-lg w-full max-w-6xl p-8">
    
    <div class="flex justify-center mb-6">
      <div class="bg-blue-600 p-4 rounded-lg">
        <img src="https://media.istockphoto.com/id/1414160809/pt/vetorial/airplane-icon-plane-flight-pictogram-transport-symbol-travel.jpg?s=612x612&w=0&k=20&c=YFAp5pnsR_fHynqYZ8d7nWjLDUtps7vaITIfYJBkmKY=" alt="Logo NextStop" class="h-10">
      </div>
    </div>

    <h3 class="text-2xl font-bold text-blue-400 text-center mb-6">Viagens Cadastradas</h3>

    {% if viagens %}
      <div class="overflow-x-auto">
        <table class="min-w-full border border-gray-600 rounded-lg overflow-hidden">
          <thead class="bg-blue-600 text-white">
            <tr>
              <th class="px-4 py-3 text-left">Destino</th>
              <th class="px-4 py-3 text-left">Data de Ida</th>
              <th class="px-4 py-3 text-left">Data de Volta</th>
              <th class="px-4 py-3 text-left">Horário</th>
              <th class="px-4 py-3 text-left">Lugar</th>
              <th class="px-4 py-3 text-center">Ações</th>
            </tr>
          </thead>
          <tbody class="bg-gray-700 divide-y divide-gray-600">
            {% for viagem in viagens %}
              <tr>
                <td class="px-4 py-3">{{ viagem.destino }}</td>
                <td class="px-4 py-3">{{ viagem.data_ida }}</td>
                <td class="px-4 py-3">{{ viagem.data_volta }}</td>
                <td class="px-4 py-3">{{ viagem.horario }}</td>
                <td class="px-4 py-3">{{ viagem.lugar }}</td>
                <td class="px-4 py-3 text-center">
                  <form action="{% url 'deletar_viagem' viagem.id %}" method="POST" class="inline-block">
                    {% csrf_token %}
                    <button type="submit" class="text-red-400 hover:underline mr-2">Deletar</button>
                  </form>
                  <a href="{% url 'editar_viagem' viagem.id %}" class="text-blue-400 hover:underline">Editar</a>
                </td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    {% else %}
      <p class="text-center text-gray-400">Nenhuma viagem cadastrada ainda.</p>
    {% endif %}

    <div class="mt-8">
      <button onclick="location.href='{% url 'homeadm' %}'"
              class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 rounded-lg">
        Voltar ao Painel
      </button>
    </div>
  </div>
</body>
</html>